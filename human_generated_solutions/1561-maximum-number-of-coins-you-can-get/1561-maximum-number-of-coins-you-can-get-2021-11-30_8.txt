class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        piles = sorted(piles)
        start = len(piles) // 3
        ans = 0
        
        while start < len(piles):
            ans += piles[start]
            start += 2
			
        return ans