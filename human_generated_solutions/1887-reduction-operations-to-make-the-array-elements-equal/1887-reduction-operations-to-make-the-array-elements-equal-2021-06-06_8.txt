class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        dp=[0]*len(nums)
        nums.sort()
        for i in range(1,len(nums)):
            if nums[i]==nums[i-1]:
                dp[i]=dp[i-1]
            else:
                dp[i]=1+dp[i-1]
        return sum(dp)