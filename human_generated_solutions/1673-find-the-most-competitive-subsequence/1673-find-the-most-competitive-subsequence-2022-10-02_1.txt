class Solution:
    def mostCompetitive(self, nums: List[int], k: int) -> List[int]:

        n, ans = len(nums)-k, []

        for num in nums:
            while n and ans and num < ans[-1]:
                ans.pop()
                n -= 1            
            ans.append(num)

        return ans[:k]