class Solution:
    def Traversal(self, root: Optional[TreeNode]):
		stack = []
		node = root 
		while node or stack:
			while node:
				stack.append(node)
				node = node.left
			node = stack.pop()
			node = node.right