class Solution:
    def makeEqual(self, words: List[str]) -> bool:
        _ = \'\'.join(words)
        for c in set(_):
            if _.count(c) % len(words) != 0:
                return False
        return True