arr=[]
        
        N=len(grid)
        for ii in range(N):
            a=[]
            for i in range(len(grid)):
                for j in range(len(grid[i])):
                    if j==ii:
                        a.append(grid[i][j])
            arr.append(a)
            a=[]
        # print(arr)
        count=0
        for xx in range(len(grid)):
            for yy in range(len(arr)):
                if grid[xx]==arr[yy]:
                    count+=1
        return count