def findSecondMinimumValue(self, root: TreeNode) -> int:
        def inorder(n):
            if n:
                inorder(n.left)
                output.add(n.val)
                inorder(n.right)
        output = set()
        inorder(root)
        if len(output) == 1:
            return -1
        return sorted(output)[1]