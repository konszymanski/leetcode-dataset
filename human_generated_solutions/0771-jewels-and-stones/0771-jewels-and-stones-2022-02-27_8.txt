class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        stones = list(stones)
        jewels = list(jewels)
        
        match = len([i for i in stones if i in jewels])
        
        return match