class Solution:
    def makeFancyString(self, s: str) -> str:
        if len(s) < 3:
            return s
        result = s[0]+s[1]
        for i in range(2,len(s)):
            if s[i] != result[-1] or s[i] != result[-2]:
                result += s[i]
        return result