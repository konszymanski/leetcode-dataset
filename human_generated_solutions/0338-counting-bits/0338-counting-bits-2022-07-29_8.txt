class Solution:
    def countBits(self, n: int) -> List[int]:
        dp = [0]
        for i in range(1, n+1):
            bits_count = dp[i // 2]
			if i & 1:                                      # is i odd number
                bits_count += 1
            dp.append(bits_count)
        return dp