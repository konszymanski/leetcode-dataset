class Solution:
    def arraySign(self, nums: List[int]) -> int:
        signs = False
        for x in nums:
            if x == 0 : return 0
            signs = signs ^ (x < 0)
        if signs : return -1
        else: return 1