# preprocess code
	stack = []
	for c in s:
		if c == \')\':
			if not stack: return False
			if stack[-1] == \'(\': stack.pop()
			else: stack.append(\')\')
		else:
			stack.append(c)			
	s = "".join(stack)