class Solution:
    def sortArrayByParityII(self, A: List[int]) -> List[int]:
		ans = [None] * len(A)
		k = 0
		t = 1
		for i in A:
			if i % 2 == 0:
				ans[k] = i
				k += 2
			else:
				ans[t] = i
				t += 2
		return ans