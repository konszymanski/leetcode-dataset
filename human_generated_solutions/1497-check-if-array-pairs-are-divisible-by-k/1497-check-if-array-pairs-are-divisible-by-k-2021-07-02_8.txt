freq=[0]*k
        for i in range(len(arr)):
            freq[arr[i]%k]+=1
        count=(freq[0])//2
        if len(freq)==1:
            return count == len(arr)/2
        
        i=0
        for i in range(1,len(freq)//2+1):
            count+=min(freq[i],freq[len(freq)-i])
        if k&1>0:
            count+=min(freq[i+1],freq[len(freq)-i-1])
        else:
            count+=freq[i+1]//2
        return count == len(arr)/2