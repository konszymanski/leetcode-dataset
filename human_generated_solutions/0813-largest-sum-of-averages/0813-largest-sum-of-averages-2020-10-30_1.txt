def dfs(A, k):
	if not A:
		return 0
	if k == 1:
		return mean(A)
	largest = 0
	for i in range(1, len(A)):
		curr = mean(A[:i]) + dfs(A[i:], k - 1)
		largest = max(largest, curr)
	return largest
return dfs(A, K)