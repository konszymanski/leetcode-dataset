if not logs:
            return 0
        
        stk = []
        
        for log in logs:
            if log == \'../\':
                if stk:
                    stk.pop()
            elif log == \'./\':
                continue
            else:
                stk.append(log)
        
        return len(stk)