class Solution:
    def minIncrementForUnique(self, A: List[int]) -> int:
        A.sort()
        count = 0 
        p = 1
        while p < len(A):
            if A[p] <= A[p-1]:
                count += A[p-1] - A[p] + 1
                A[p] = A[p-1] + 1
            p += 1
        return count