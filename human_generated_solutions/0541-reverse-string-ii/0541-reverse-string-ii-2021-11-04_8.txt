class Solution:
    def reverseStr(self, s: str, k: int) -> str:
        x=(len(s)//(2*k))+1
        a=0
        while(x!=0):
            x=x-1
            y=s[a:a+k]
            s=s.replace(y,y[::-1])
            a=a+2*k
            #print(y,s,a)
        return(s)