class Solution:
    def findCenter(self, edges: List[List[int]]) -> int:
        s=set()
        for i,j in edges:
            if i in s:
                return i
            else:
                s.add(i)
            if j in s:
                return j
            else:
                s.add(j)