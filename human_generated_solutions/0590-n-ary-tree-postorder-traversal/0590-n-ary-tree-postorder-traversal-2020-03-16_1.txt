def iterative(self, root):
        if not root: return []
        stack = [root]
        out = []
        while len(stack):
            top = stack.pop()
            out.append(top.val)
            stack.extend(top.children or [])
        return out[::-1]