def canMakeArithmeticProgression(self, arr: List[int]) -> bool:
	arr.sort()
	diff = arr[0] - arr[1]
	i = 1
	while i < len(arr)-1:
		if arr[i] - arr[i+1] != diff:
			return False
			break
		else:
			i += 1
	return True