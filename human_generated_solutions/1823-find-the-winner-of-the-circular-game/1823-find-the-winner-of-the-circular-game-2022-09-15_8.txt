class Solution:
    def findTheWinner(self, n: int, k: int) -> int:
        q = deque(range(n))
        while len(q) > 1:
            for _ in range(k - 1):
                q.append(q.popleft())
            q.popleft()
        return q[0] + 1
    # Time: O(n * k)
    # Space: O(n)