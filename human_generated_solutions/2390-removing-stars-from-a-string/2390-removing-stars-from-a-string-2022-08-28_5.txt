def removeStars(self, s: str) -> str:
    st=[]
    for i in range(len(s)-1,-1,-1):
        if not st:
            st.append(s[i])
        elif st[-1]==\'*\' and s[i]!=\'*\':
                st.pop()
        else:
            st.append(s[i])
    st=\'\'.join(st)
    return st[::-1]