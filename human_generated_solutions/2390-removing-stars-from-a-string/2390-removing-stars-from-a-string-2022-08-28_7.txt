class Solution:
    def removeStars(self, s: str) -> str:
        stack = []
        
        for c in s:
            stack.append(c)
            if stack[-1] == "*":
                stack.pop()
                if stack: stack.pop()
        
        return "".join(stack)