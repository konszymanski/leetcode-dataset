def minBitFlips(self, start: int, goal: int) -> int:
        
        n = start ^ goal 
        count = 0

        #counting set bits
        while n:
            n = n & (n-1) 
            count += 1

        return count