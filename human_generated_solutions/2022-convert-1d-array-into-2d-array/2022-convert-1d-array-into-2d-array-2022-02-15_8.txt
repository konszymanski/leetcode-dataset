class Solution(object):
    def construct2DArray(self,original, m, n):
	
        result = []

        if len(original)==m*n:
            for row in range(m):
                result.append(original[n*row:n*row+n])

        return result