store = \'123456789\'
ans = []
start = len(str(low))
end = len(str(high))

while start <= end:
	for i in range(len(store) - start + 1):
		curr = int(store[i:i+start])
		if low <= curr <= high:
			ans.append(curr)

	start += 1
return ans