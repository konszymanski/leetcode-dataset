class Solution(object):
    def maxWidthRamp(self, A):
        """
        :type A: List[int]
        :rtype: int
        """
        return reduce(lambda x, y: (max(x[0], y[1] - x[1]), min(y[1], x[1])), sorted([(val, i) for i, val in enumerate(A)]), (0, len(A) + 1))[0]