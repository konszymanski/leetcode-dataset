if k>len(arr)//2:
	return(max(arr))
else:
	count=0
	curr_max=arr[0]
	for i in range(1,len(arr)):
	  if count==k:
		return(curr_max)
		break
	  else:
		if arr[i]>curr_max:
		  curr_max=arr[i]
		  count=1
		else:
		  count+=1
return(curr_max)