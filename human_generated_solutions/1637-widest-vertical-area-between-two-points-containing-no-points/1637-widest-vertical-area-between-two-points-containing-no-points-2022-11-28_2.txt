class Solution:
    def maxWidthOfVerticalArea(self, points: List[List[int]]) -> int:
        points.sort()
        m,n=0,len(points)
        for i in range(1,n):
            m=max(m,(points[i][0]-points[i-1][0]))
        return m