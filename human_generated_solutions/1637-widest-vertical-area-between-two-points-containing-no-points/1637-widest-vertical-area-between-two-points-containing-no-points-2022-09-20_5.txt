target=[]
        
        for i in range(len(points)):
            target.append(points[i][0])
        target.sort()
        
        max_diff=0
        for j in range(len(target)-1):
            if abs(target[j]-target[j+1])>max_diff:
                max_diff=abs(target[j]-target[j+1])
        return max_diff