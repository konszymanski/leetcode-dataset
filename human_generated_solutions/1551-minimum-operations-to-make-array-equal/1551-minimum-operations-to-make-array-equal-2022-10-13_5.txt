nums = []        
        for i in range(n):
            nums.append((2 * i) + 1)
        
        res, mid = 0, 0
        if n % 2 == 0:
            mid = n
        else:
            mid = nums[len(nums) // 2]
        for num in nums:
            res += abs(num - mid)
        return res // 2