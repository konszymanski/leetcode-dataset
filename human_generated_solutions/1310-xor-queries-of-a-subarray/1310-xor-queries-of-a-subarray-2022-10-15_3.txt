class Solution:
    def xorQueries(self, arr, q, cache= {}):
        a = [0]
        for i in range(len(arr)):
            a.append(a[-1]^arr[i])
        res = []
        for l,r in q:
            res.append(a[r+1]^a[l])
        return res