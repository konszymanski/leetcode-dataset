class Solution:
    def findSmallestSetOfVertices(self, n: int, edges: List[List[int]]) -> List[int]:
        ed = set(range(n))
        for i, j in edges:
            if j in ed:
                ed.remove(j)
        return list(ed)