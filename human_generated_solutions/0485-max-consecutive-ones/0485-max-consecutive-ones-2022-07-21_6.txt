class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        count, max_ones = 0, 0
        for num in nums:
            if num != 1:
                count = 0
            else:
                count += 1
            max_ones = max(count, max_ones)
        return max_ones