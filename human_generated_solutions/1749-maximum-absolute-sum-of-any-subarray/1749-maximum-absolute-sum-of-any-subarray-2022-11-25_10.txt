class Solution:
    def maxAbsoluteSum(self, nums: List[int]) -> int:
        ans = 0
        maxi=0
        mini=0
        for x in nums: 
            maxi = max(maxi + x, 0)
            mini = min(mini + x, 0)
            ans = max(ans, maxi, -mini)
        return ans