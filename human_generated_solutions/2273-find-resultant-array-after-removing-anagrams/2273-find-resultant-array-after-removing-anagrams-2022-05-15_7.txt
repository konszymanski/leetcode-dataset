class Solution:
    def removeAnagrams(self, words: List[str]) -> List[str]:
        if len(words)==1:
            return words
        i = 1 
        while i<len(words):
            if sorted(list(words[i]))==sorted(list(words[i-1])):
                words.pop(i)
            else:
                i+=1 
        return words