class Solution:
    def getDescentPeriods(self, prices: List[int]) -> int:
        count=0
        ans=0;i=1
        while i<len(prices):
            if prices[i-1]-1==prices[i]:
                count+=1
                ans+=count
            else:
                count=0
            i+=1
        ans+=len(prices)
        return ans