def optimalDivision(self, nums: List[int]) -> str:
	ans = ""
	if(len(nums) == 1):
		return str(nums[0])
	if(len(nums) == 2):
		return str(nums[0]) + \'/\' + str(nums[1])
	for i in range(1, len(nums)):  
		ans += \'/\' + str(nums[i])
	ans = str(nums[0]) +\'/\' + \'(\' + ans[1:] + \')\'
	return ans