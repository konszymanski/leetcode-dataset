class Solution:
    def maxPower(self, s: str) -> int:
        i,j=0,0
        ans=0
        while(j<len(s)):
            if s[i]==s[j]:
                ans=max(ans,j+1-i)
                j+=1
            else:
                i=j
        return(ans)