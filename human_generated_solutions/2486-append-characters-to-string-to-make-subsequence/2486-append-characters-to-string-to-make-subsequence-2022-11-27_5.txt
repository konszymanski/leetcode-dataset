class Solution {
public:
    int appendCharacters(string s, string t) {
        int i = 0; 
        for (auto& ch : s) 
            if (i < t.size() && ch == t[i]) ++i; 
        return t.size() - i; 
    }
};