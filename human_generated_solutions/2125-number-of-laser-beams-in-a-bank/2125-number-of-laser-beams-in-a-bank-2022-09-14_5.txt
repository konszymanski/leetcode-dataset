arr=[]
        
        for i in range(len(bank)):
            count=0
            for j in range(len(bank[i])):
                if bank[i][j]==\'1\':
                    count+=1
            arr.append(count)

        ans=[]
        
        for ii in range(len(arr)):
            if arr[ii]!=0:
                ans.append(arr[ii])
        
        anss=0
        
        for x in range(len(ans)-1):
            anss+=ans[x]*ans[x+1]
        return anss