class Solution:
    def numberOfBeams(self, bank: List[str]) -> int:
        pre = 0
        nn = 0
        ans = 0
        for i in bank:
            nn=i.count(\'1\')
            if nn:
                ans+=nn*pre
                pre= nn
        return ans