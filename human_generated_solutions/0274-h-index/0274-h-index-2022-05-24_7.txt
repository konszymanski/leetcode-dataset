class Solution:
    def hIndex(self, citations: List[int]) -> int:
        citations.sort(key= lambda x:-x)
        ans = 0
        for i in range(len(citations)):
            if i + 1 > citations[i]:
                break
            ans = i + 1
        return ans