class Solution:
    def isOneBitCharacter(self, bits: List[int]) -> bool:
        i = 0
        k = []
        while i<=len(bits)-1:
            if bits[i]==1:
                i+=2
                k.append(2)
            else:
                i+=1
                k.append(1)
        return  k[-1]==1