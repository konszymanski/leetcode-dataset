class Solution:
    def zeroFilledSubarray(self, nums: List[int]) -> int:
        ans, currentCountOF0 = 0, 0
        for num in nums:
            if num == 0:
                currentCountOF0 += 1
                ans += currentCountOF0
            else:
                currentCountOF0 = 0
        return ans