target = defaultdict(int)
        for w in words2:
            wd = Counter(w)
            for k, v in wd.items():
                target[k] = max(target[k], wd[k])
            
        res = []
        for w in words1:
            wd = Counter(w)
            for k, v in target.items():
                wd[k] -= v
                if wd[k] < 0:
                    break
            else:
                res.append(w)
                
        return res