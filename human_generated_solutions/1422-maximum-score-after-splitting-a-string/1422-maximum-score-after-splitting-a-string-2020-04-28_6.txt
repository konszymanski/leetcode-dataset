class Solution:
    def maxScore(self, s: str) -> int:
        ones = sum(c == \'1\' for c in s)
        ans = zeros = 0
        for c in s[:-1]:
            if c == \'0\':
                zeros += 1
            else:
                ones -= 1
            ans = max(ans, zeros + ones)
        return ans