import random
class Solution:
    nums = []

    def __init__(self, nums: List[int]):
        self.nums = nums

    def reset(self) -> List[int]:
        return self.nums

    def shuffle(self) -> List[int]:
        temp = self.nums.copy()
#         len_temp = len(temp)
        
#         for i in range(len_temp):
#             rand_value = random.randint(0,len_temp-1)
#             t = temp[i]
#             temp[i] = temp[rand_value]
#             temp[rand_value] = t
            
        random.shuffle(temp)
        
        
        return temp


# Your Solution object will be instantiated and called as such:
# obj = Solution(nums)
# param_1 = obj.reset()
# param_2 = obj.shuffle()