mp = defaultdict(list)
output = []
for i, grp in enumerate(groupSizes):
	mp[grp].append(i)
	 """
	 once length of list in map is equal to group length,
	 append it to output and delete it from map.
	 """
	if len(mp[grp])==grp:
		output.append(mp[grp])
		del mp[grp]
return output