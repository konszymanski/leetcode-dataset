class Solution:
    def maxIceCream(self, costs: List[int], coins: int) -> int:
        costs.sort()
        sums=0
        c=0
        for i in costs:
            if sums+i<=coins:
                sums= sums+i
                c=c+1
        return(c)