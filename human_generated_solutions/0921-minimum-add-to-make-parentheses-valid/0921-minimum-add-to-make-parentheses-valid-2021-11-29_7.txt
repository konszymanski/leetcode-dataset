class Solution:
    def minAddToMakeValid(self, s: str) -> int:
        count = 0
        st = []
        for char in s:
            if char == "(":
                st.append(char)
            else:
                if not st:
                    count += 1
                else:
                    curChar = st.pop()

        while st:
            st.pop()
            count += 1
            
        return count