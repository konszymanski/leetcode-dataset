def maxChunksToSorted(self, arr: List[int]) -> int:
	sum1, sum2 = 0, 0
	l = len(arr)
	count = 0

	for i in range(0, l):
		sum1+=i
		sum2+=arr[i]
		if sum1==sum2:
			count+=1
			sum1, sum2 = 0, 0
	return count