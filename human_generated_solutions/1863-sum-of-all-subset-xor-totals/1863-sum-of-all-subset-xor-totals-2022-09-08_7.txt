def subsetXORSum(self, nums: List[int]) -> int:
        ans = 0
        for ss in range(1<<len(nums)):
            xsum = 0
            for num in nums:
                if ss & 1:
                    xsum ^= num
                ss >>= 1
            ans += xsum
        return ans