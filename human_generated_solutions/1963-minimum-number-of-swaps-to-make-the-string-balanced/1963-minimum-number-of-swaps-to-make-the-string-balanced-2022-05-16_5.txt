class Solution:
    def minSwaps(self, s: str) -> int:
        res = 0
        stack = 0
        for b in s:
            if b == \'[\':
                stack += 1
            elif not stack:
                stack += 1
                res += 1
            else:
                stack -= 1
        return res