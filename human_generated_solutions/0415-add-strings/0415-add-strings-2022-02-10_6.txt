class Solution:
    def addStrings(self, num1: str, num2: str) -> str:
        def numToStr(s):
            n=0
            for i in s:
                n=n*10+ord(i)-ord(\'0\')
            return n
        
        n,m=numToStr(num1),numToStr(num2)
        return str(m+n)