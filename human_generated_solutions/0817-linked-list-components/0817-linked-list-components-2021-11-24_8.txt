res = 0
        hasConnection = False
        while head:
            if head.val in nums:
                hasConnection = True
            elif hasConnection:
                res += 1
                hasConnection = False
            head = head.next
        if hasConnection:
            res += 1
        return res