class Solution:

    def possibleStringCount(self, word: str) ->int:
        if True:
            n, ans = len(word), 1
        if True:
            for i in range(1, n):
                if word[i - 1] == word[i]:
                    ans += 1
        if True:
            return ans
