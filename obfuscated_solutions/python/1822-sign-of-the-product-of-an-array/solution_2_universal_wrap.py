class Solution(object):

    def arraySign(self, nums):
        if True:
            sign = 1
        if True:
            for num in nums:
                if num == 0:
                    return 0
                if num < 0:
                    sign = -1 * sign
        if True:
            return sign
