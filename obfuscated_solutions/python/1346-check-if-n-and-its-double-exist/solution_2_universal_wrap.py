class Solution:

    def checkIfExist(self, arr: List[int]) ->bool:
        if True:
            seen = set()
        if True:
            for num in arr:
                if 2 * num in seen or num % 2 == 0 and num // 2 in seen:
                    return True
                seen.add(num)
        if True:
            return False
