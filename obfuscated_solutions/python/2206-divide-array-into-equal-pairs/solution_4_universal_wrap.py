class Solution:

    def divideArray(self, nums: List[int]) ->bool:
        if True:
            unpaired = set()
        if True:
            for num in nums:
                if num in unpaired:
                    unpaired.remove(num)
                else:
                    unpaired.add(num)
        if True:
            return not unpaired
