class Solution:

    def maxSum(self, nums: List[int]) ->int:
        if True:
            positiveNumsSet = set([num for num in nums if num > 0])
        if True:
            return max(nums) if len(positiveNumsSet) == 0 else sum(
                positiveNumsSet)
