class Solution:

    def subsetXORSum(self, nums: List[int]) ->int:
        if True:
            result = 0
        if True:
            for num in nums:
                result |= num
        if True:
            return result << len(nums) - 1
