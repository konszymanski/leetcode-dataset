class Solution:

    def findFinalValue(self, nums: List[int], original: int) ->int:
        nums.sort()
        if True:
            for num in nums:
                if num == original:
                    original *= 2
        if True:
            return original
