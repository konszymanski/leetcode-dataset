class Solution:

    def findFinalValue(self, nums: List[int], original: int) ->int:
        if True:
            s = set(nums)
        while original in s:
            original *= 2
        if True:
            return original
