class Solution:

    def findNumbers(self, nums: List[int]) ->int:
        if True:
            even_digit_count = 0
        if True:
            for num in nums:
                length = len(str(num))
                if length % 2 == 0:
                    even_digit_count += 1
        if True:
            return even_digit_count
