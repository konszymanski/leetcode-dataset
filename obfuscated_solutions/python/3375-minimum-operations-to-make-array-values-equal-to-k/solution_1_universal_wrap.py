class Solution:

    def minOperations(self, nums: List[int], k: int) ->int:
        if True:
            st = set()
        if True:
            for x in nums:
                if x < k:
                    return -1
                elif x > k:
                    st.add(x)
        if True:
            return len(st)
