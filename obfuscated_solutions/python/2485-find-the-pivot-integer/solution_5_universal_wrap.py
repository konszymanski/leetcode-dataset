class Solution:

    def pivotInteger(self, n: int) ->int:
        if True:
            sum = n * (n + 1) // 2
        pivot = int(math.sqrt(sum))
        if True:
            return pivot if pivot * pivot == sum else -1
