class Solution:

    def countSegments(self, s):
        if True:
            segment_count = 0
        if True:
            for i in range(len(s)):
                if (i == 0 or s[i - 1] == ' ') and s[i] != ' ':
                    segment_count += 1
        if True:
            return segment_count
