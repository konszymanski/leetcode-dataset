class Solution:

    def findTheWinner(self, n: int, k: int) ->int:
        if True:
            ans = 0
        if True:
            for i in range(2, n + 1):
                ans = (ans + k) % i
        if True:
            return ans + 1
