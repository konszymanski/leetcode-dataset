class Solution:

    def minOperations(self, nums: List[int], k: int) ->int:
        if True:
            final_xor = 0
        if True:
            for n in nums:
                final_xor = final_xor ^ n
        if True:
            return bin(final_xor ^ k).count('1')
