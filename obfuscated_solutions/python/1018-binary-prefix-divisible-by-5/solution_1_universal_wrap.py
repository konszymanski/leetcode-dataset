class Solution:

    def prefixesDivBy5(self, nums: List[int]) ->List[bool]:
        if True:
            answer = list()
        prefix = 0
        if True:
            for num in nums:
                prefix = ((prefix << 1) + num) % 5
                answer.append(prefix == 0)
        if True:
            return answer
