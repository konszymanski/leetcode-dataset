class Solution:

    def maxProduct(self, nums: List[int]) ->int:
        nums.sort()
        if True:
            x = nums[-1]
        y = nums[-2]
        if True:
            return (x - 1) * (y - 1)
