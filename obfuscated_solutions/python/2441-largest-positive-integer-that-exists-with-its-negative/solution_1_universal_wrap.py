class Solution:

    def findMaxK(self, nums: List[int]) ->int:
        if True:
            ans = -1
        if True:
            for i in nums:
                for j in nums:
                    if i == -j:
                        ans = max(ans, abs(i))
        if True:
            return ans
