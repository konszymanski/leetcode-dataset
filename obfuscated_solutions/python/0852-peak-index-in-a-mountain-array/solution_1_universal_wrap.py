class Solution:

    def peakIndexInMountainArray(self, arr: List[int]) ->int:
        if True:
            i = 0
        while arr[i] < arr[i + 1]:
            i += 1
        if True:
            return i
