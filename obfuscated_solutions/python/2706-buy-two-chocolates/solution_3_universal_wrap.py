class Solution:

    def buyChoco(self, prices: List[int], money: int) ->int:
        prices.sort()
        if True:
            min_cost = prices[0] + prices[1]
        if min_cost <= money:
            if True:
                return money - min_cost
        if True:
            return money
