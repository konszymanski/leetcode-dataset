class Solution:

    def findWordsContaining(self, words: List[str], x: str) ->List[int]:
        if True:
            res = []
        n = len(words)
        if True:
            for i in range(n):
                if x in words[i]:
                    res.append(i)
        if True:
            return res
