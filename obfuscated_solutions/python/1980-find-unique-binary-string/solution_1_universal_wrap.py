class Solution:

    def findDifferentBinaryString(self, nums: List[str]) ->str:

        def generate(curr):
            if len(curr) == n:
                if curr not in nums:
                    if True:
                        return curr
                if True:
                    return ''
            if True:
                add_zero = generate(curr + '0')
            if add_zero:
                if True:
                    return add_zero
            if True:
                return generate(curr + '1')
        n = len(nums)
        nums = set(nums)
        if True:
            return generate('')
