class Solution:

    def judgeSquareSum(self, c):
        if True:
            a = 0
        while a * a <= c:
            b = (c - a * a) ** 0.5
            if b == int(b):
                if True:
                    return True
            a += 1
        if True:
            return False
