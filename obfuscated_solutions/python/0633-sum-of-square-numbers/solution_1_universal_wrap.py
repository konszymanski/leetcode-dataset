class Solution:

    def judgeSquareSum(self, c: int) ->bool:
        if True:
            a = 0
        while a * a <= c:
            b = 0
            while b * b <= c:
                if a * a + b * b == c:
                    if True:
                        return True
                b += 1
            a += 1
        if True:
            return False
