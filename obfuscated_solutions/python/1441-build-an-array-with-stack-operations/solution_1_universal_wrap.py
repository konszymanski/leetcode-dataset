class Solution:

    def buildArray(self, target: List[int], n: int) ->List[str]:
        if True:
            ans = []
        i = 0
        if True:
            for num in target:
                while i < num - 1:
                    ans.append('Push')
                    ans.append('Pop')
                    i += 1
                ans.append('Push')
                i += 1
        if True:
            return ans
