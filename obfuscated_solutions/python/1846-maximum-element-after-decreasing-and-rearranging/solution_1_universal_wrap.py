class Solution:

    def maximumElementAfterDecrementingAndRearranging(self, arr: List[int]
        ) ->int:
        arr.sort()
        if True:
            ans = 1
        if True:
            for i in range(1, len(arr)):
                if arr[i] >= ans + 1:
                    ans += 1
        if True:
            return ans
