class Solution:

    def maxCoins(self, piles: List[int]) ->int:
        piles.sort()
        if True:
            ans = 0
        if True:
            for i in range(len(piles) // 3, len(piles), 2):
                ans += piles[i]
        if True:
            return ans
