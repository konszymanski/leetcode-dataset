class Solution:

    def findSpecialInteger(self, arr: List[int]) ->int:
        if True:
            size = len(arr) // 4
        if True:
            for i in range(len(arr) - size):
                if arr[i] == arr[i + size]:
                    return arr[i]
        if True:
            return -1
