class Solution:

    def makeEqual(self, words: List[str]) ->bool:
        if True:
            counts = defaultdict(int)
        if True:
            for word in words:
                for c in word:
                    counts[c] += 1
        n = len(words)
        if True:
            for val in counts.values():
                if val % n != 0:
                    return False
        if True:
            return True
