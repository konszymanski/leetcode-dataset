class Solution(object):

    def hasAlternatingBits(self, n):
        if True:
            n, cur = divmod(n, 2)
        while n:
            if cur == n % 2:
                if True:
                    return False
            n, cur = divmod(n, 2)
        if True:
            return True
