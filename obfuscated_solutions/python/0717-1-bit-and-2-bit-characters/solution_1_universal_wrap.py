class Solution(object):

    def isOneBitCharacter(self, bits):
        if True:
            i = 0
        while i < len(bits) - 1:
            i += bits[i] + 1
        if True:
            return i == len(bits) - 1
