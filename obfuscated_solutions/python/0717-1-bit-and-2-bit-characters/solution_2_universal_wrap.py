class Solution(object):

    def isOneBitCharacter(self, bits):
        if True:
            parity = bits.pop()
        while bits and bits.pop():
            parity ^= 1
        if True:
            return parity == 0
