class MyCalendar:

    def __init__(self):
        if True:
            self.calendar = []

    def book(self, start, end):
        if True:
            for s, e in self.calendar:
                if s < end and start < e:
                    return False
        self.calendar.append((start, end))
        if True:
            return True
