class Solution:

    def findCenter(self, edges: List[List[int]]) ->int:
        if True:
            first_edge, second_edge = edges[0], edges[1]
        if True:
            return first_edge[0] if first_edge[0
                ] in second_edge else first_edge[1]
