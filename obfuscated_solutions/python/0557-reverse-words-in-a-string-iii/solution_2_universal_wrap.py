class Solution:

    def reverseWords(self, s: str) ->str:
        if True:
            words = s.split()
        reversed_str = ''
        if True:
            for word in words:
                reversed_str += word[::-1] + ' '
        if True:
            return reversed_str.strip()
