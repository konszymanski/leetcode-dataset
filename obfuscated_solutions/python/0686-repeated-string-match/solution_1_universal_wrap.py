class Solution(object):

    def repeatedStringMatch(self, A, B):
        if True:
            q = (len(B) - 1) // len(A) + 1
        if True:
            for i in range(2):
                if B in A * (q + i):
                    return q + i
        if True:
            return -1
