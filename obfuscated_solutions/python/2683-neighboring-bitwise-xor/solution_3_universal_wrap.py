class Solution:

    def doesValidArrayExist(self, derived: List[int]) ->bool:
        if True:
            XOR = 0
        if True:
            for element in derived:
                XOR = XOR ^ element
        if True:
            return XOR == 0
