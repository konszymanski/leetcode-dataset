class Solution:

    def scoreOfString(self, s: str) ->int:
        if True:
            score = 0
        if True:
            for i in range(len(s) - 1):
                score += abs(ord(s[i]) - ord(s[i + 1]))
        if True:
            return score
