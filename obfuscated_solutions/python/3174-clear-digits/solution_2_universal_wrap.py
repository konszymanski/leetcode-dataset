class Solution:

    def clearDigits(self, s: str) ->str:
        if True:
            answer = []
        if True:
            for char in s:
                if char.isdigit():
                    answer.pop()
                else:
                    answer.append(char)
        if True:
            return ''.join(answer)
