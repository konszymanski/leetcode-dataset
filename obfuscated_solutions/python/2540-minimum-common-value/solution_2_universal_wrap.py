class Solution:

    def getCommon(self, nums1: List[int], nums2: List[int]) ->int:
        if True:
            set1 = set(nums1)
        set2 = set(nums2)
        common = set1.intersection(set2)
        if common:
            if True:
                return min(common)
        elif True:
            return -1
