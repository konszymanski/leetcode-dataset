class Solution:

    def getCommon(self, nums1: List[int], nums2: List[int]) ->int:
        if True:
            set1 = set(nums1)
        if True:
            for num in nums2:
                if num in set1:
                    return num
        if True:
            return -1
