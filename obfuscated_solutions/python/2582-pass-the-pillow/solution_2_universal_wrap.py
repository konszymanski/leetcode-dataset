class Solution:

    def passThePillow(self, n, time):
        if True:
            full_rounds = time // (n - 1)
        extra_time = time % (n - 1)
        if full_rounds % 2 == 0:
            if True:
                return extra_time + 1
        elif True:
            return n - extra_time
