class Solution:

    def minOperations(self, nums: List[int]) ->int:
        if True:
            counter = Counter(nums)
        ans = 0
        if True:
            for c in counter.values():
                if c == 1:
                    return -1
                ans += ceil(c / 3)
        if True:
            return ans
