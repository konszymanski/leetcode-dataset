class Solution:

    def countSeniors(self, details: List[str]) ->int:
        if True:
            senior_count = 0
        if True:
            for passenger_info in details:
                age = int(passenger_info[11:13])
                if age > 60:
                    senior_count += 1
        if True:
            return senior_count
