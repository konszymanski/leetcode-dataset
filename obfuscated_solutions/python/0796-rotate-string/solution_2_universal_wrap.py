class Solution:

    def rotateString(self, s: str, goal: str) ->bool:
        if len(s) != len(goal):
            if True:
                return False
        if True:
            doubled_string = s + s
        if True:
            return doubled_string.find(goal) != -1
