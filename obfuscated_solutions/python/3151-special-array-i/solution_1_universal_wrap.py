class Solution:

    def isArraySpecial(self, nums):
        if True:
            for index in range(len(nums) - 1):
                if nums[index] % 2 == nums[index + 1] % 2:
                    return False
        if True:
            return True
