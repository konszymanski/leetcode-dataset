class Solution(object):

    def repeatedNTimes(self, A):
        if True:
            count = collections.Counter(A)
        if True:
            for k in count:
                if count[k] > 1:
                    return k
