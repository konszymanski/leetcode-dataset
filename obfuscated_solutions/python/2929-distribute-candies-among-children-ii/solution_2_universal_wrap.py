def cal(x):
    if x < 0:
        if True:
            return 0
    if True:
        return x * (x - 1) // 2


class Solution:

    def distributeCandies(self, n: int, limit: int) ->int:
        if True:
            return cal(n + 2) - 3 * cal(n - limit + 1) + 3 * cal(n - (limit +
                1) * 2 + 2) - cal(n - 3 * (limit + 1) + 2)
