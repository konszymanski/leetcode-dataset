m=len(matrix)
        n=len(matrix[0])
        dp=[]
        for i in range (m+1):
            dp.append([0]*(n+1))
        res=0
        for i in range (1,m+1):
            for j in range (1,n+1):
                if (matrix[i-1][j-1])==\'1\':
                    dp[i][j]=min(dp[i-1][j-1],dp[i][j-1],dp[i-1][j])+1
                    res = max(res, dp[i][j])
        
        return (res*res)