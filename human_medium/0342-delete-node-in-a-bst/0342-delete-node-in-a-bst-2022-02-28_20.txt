# assuming no null input
def findMaximum(node: TreeNode) -> TreeNode:
	prev = None
	while root:
		prev = root
		root = root.right
	return prev
def findMinimum(node: TreeNode) -> TreeNode:
	prev = None
	while root:
		prev = root
		root = root.left
	return prev