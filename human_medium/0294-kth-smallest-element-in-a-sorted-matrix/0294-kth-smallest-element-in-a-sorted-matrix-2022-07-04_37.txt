def kthSmallest(self, matrix: List[List[int]], k: int) -> int:
	heap = []
	n = len(matrix)
	for i in range(n):
		heappush(heap, (matrix[i][0], matrix[i][1:]))

	while(heap):
		ele, row = heappop(heap)
		if(row):
			heappush(heap, (row[0], row[1:]))
		k -= 1
		if(k == 0):
			return ele