class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        buy, sell, cool = -math.inf, 0, 0
        
        for x in prices:
            buy, sell, cool = (
                max(buy, cool-x),
                max(sell, buy+x),
                sell
            )
        
        return sell