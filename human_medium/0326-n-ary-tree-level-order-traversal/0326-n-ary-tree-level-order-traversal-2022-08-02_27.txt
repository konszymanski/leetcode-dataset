def levelOrder(self, root: \'Node\') -> List[List[int]]:
	if(not root): return
	s = [root]
	ans = []
	i = 0
	while(s):
		ans.append([i.val for i in s])
		l = len(s)
		for _ in range(l):
			for child in s.pop(0).children:
				s.append(child)
	return ans