class Solution:
    def wiggleMaxLength(self, nums: List[int]) -> int:
        ma=1
        mi=1
        for i in range(1,len(nums)):
            if nums[i]>nums[i-1]:
                ma=mi+1
            elif nums[i]<nums[i-1]:
                mi=ma+1
        return max(ma,mi)