def validUtf8(self, data: List[int]) -> bool:
        
        count = 0
        i=0
		
        # check the bytes of the number
        def calculateBytes(binary_val):
            if(binary_val.startswith(\'0\')):
                return 0
            elif(binary_val.startswith(\'110\')):
                return 1
            elif(binary_val.startswith(\'1110\')):
                return 2
            elif(binary_val.startswith(\'11110\')):
                return 3
            return -1
        # iterate over array
        while(i<len(data)):
            
			#get the 8 bit binary form of a digit 
            num_in_byte = str(format(data[i], \'08b\')) 
            
            if(count==0):
                count = calculateBytes(num_in_byte) #calculate the bytes
                if(count == -1):
                    return False
			
			#checking next numbers according to the instructions given in the question
            while(count > 0): 
                i += 1
                count -=1
                if(str(i < len(data) and format(data[i], \'08b\')).startswith(\'10\')):
                    pass
                else:
                    return False
            i += 1 
        return True