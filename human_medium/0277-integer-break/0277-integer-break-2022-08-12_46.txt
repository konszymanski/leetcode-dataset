class Solution:
    def integerBreak(self, n: int) -> int:
        dp = [i for i in range(n+1)]
        for i in range(4,n+1):
            dp[i] = max(dp[i], dp[i-2]*2, dp[i-3]*3)
        return max(dp[i]*dp[n-i] for i in range(1, min(3+1,n)))