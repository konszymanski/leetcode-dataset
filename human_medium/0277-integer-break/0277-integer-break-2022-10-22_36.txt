class Solution:
    def integerBreak(self, n: int) -> int:
        dp = [0] + [1] * n 
        for i in range(3,n+1):
            dp[i] = max(max(i-2,dp[i-2]) * 2, max(i-3,dp[i-3]) * 3)
        return dp[n]