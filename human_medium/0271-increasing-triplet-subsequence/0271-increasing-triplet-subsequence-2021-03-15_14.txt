class Solution:
    def increasingTriplet(self, nums: List[int]) -> bool:
        min1=min2=math.inf
        for v in nums:
            if v<min1:
                min1=v
            elif min1<v<min2:
                min2=v
            elif min2<v:
                return True
        return False