class Solution:
    def increasingTriplet(self, nums: List[int]) -> bool:
        first = float("inf")
        second = float("inf")
        for num in nums:
            if first > num:
                first = num
            if first < num and num < second:
                second = num
            if num > second:
                return True
        return False