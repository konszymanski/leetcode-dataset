prd = 1
        zs = 0
        for n in nums:
            if n == 0:
                zs += 1
            else:
                prd *= n
        
        if zs == 1:
            return [prd if n == 0 else 0 for n in nums]
        if zs > 1:
            return [0] * len(nums)
        else:
            return [prd // n for n in nums]