def addlists(node1, node2):
            
            if not node1:
                return node2
            if not node2:
                return node1
            addlists(node1.next, node2.next)
            val = node1.val + node2.val + self.carry
            self.carry = val//10
            val = val%10
            node1.val = val