class Solution:
    def eraseOverlapIntervals(self, iv: List[List[int]]) -> int:
        n = len(iv)
        iv.sort(key = lambda x: x[1])
        stk = [iv[0]]
        for i in range (1,n):
            if stk[-1][1] <= iv[i][0]:
                stk.append(iv[i])
        return n - len(stk)