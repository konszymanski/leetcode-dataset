r = re.compile(\' *([-+]?\\d+)\')

bot = - pow(2, 31)
top = pow(2, 31) - 1

class Solution:
  def myAtoi(self, s):
    m = r.match(s)
    if not m: return 0
    x = int(m.group(1))
    return max(bot, min(x, top))