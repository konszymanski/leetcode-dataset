class Solution {
    public int minMoves2(int[] nums) {
        Arrays.sort(nums);
        int total_moves = 0, mid = nums.length/2;
        for(int i=0; i<nums.length; i++){
            total_moves += Math.abs(nums[i] - nums[mid]);
        }
        return total_moves;
    }
}