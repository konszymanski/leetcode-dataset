class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        idx = int(len(nums)/2)
        median = nums[idx]
        output = 0
        for num in nums:
            diff = abs(median-num)
            output += diff
        return output