class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        result, left, right = 0, 0, len(nums)-1
        while left < right:
            result += (nums[right]-nums[left])
            left += 1
            right -= 1
        return result