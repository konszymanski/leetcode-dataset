class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        mid = nums[len(nums)//2]
        minMoves = 0
        for i in nums:
            if i < mid:
                minMoves += mid - i
            elif i > mid:
                minMoves += i -mid
                
        return minMoves