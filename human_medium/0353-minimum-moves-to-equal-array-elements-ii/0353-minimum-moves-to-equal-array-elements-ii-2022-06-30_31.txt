class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        n = len(nums)
        ans = 0
        med = nums[n//2]
        for i in range(n):
            ans += abs(nums[i] - med)
        return ans