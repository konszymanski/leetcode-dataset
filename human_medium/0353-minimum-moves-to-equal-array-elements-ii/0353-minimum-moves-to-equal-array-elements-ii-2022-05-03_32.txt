class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        mid = nums[int(len(nums) / 2)]
        steps = 0
        for n in nums:
            steps += abs(mid - n)
            
        return steps