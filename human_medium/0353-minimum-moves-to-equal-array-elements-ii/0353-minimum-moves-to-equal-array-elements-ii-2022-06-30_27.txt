class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        n=len(nums)
        ans=0
        for i in range(n//2):
            ans+=(nums[n-i-1]-nums[i])
        return ans