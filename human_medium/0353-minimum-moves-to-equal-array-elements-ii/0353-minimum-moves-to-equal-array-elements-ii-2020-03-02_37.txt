class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        """
        Time O(nlogn)
        Space O(1)
        """
        nums.sort()
        sums=0
        for num in nums:
            sums+=abs(nums[len(nums)//2]-num)
        return sums