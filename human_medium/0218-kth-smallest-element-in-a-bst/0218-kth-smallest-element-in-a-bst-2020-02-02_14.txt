def kthSmallest(self, root, k):
		stack = []
		
        while True:
            if root:
                stack.append(root)
                root = root.left
            elif stack:
                root = stack.pop()
                k -= 1
                
                if k == 0:
                    return root.val
                root = root.right
            else:
                break