class Solution:
    def frequencySort(self, s: str) -> str:
        d={}
        for i in list(set(s)):
            d[i]=s.count(i)
        
        import operator
        sd= sorted(d.items(), key=operator.itemgetter(1),reverse=True)
		#sorting the dictionary wrt values in descending order
		
        a=\'\'
        
        for i in sd:
            a+=(i[0]*i[1])
			
        return a