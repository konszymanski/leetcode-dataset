def frequencySort(self, s: str) -> str:
    heap = []
    heapify(heap)
    for k, v in Counter(s).items():
        heappush(heap, (-v, k))
    result = ""
    while len(heap) > 0:
        n, c = heapq.heappop(heap)
        result += c*(-n)
    return result