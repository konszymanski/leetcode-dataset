class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        prevNums = set()
        for n in nums:
            if n in prevNums: return n
            else: prevNums.add(n)
        return float("inf")