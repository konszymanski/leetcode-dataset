class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        n = len(nums)
        
        for i in range(n):
            index = (nums[i] - 1)%n
            nums[index] += n
            
        for i in range(n):
            if nums[i] > 2*n:
                return i+1