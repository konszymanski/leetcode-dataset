class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
         store = set()
       
         for i in nums:
             lenSB = len(store)
             store.add(i)
           
             if lenSB == len(store):
                 return i