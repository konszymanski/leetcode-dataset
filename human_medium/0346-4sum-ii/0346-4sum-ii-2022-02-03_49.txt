hashMap = defaultdict(int)
        for num1 in nums1:
            for num2 in nums2:
                hashMap[num1+num2] += 1
        count = 0
        for num3 in nums3:
            for num4 in nums4:
                    count += hashMap[(-(num3+num4))]
        
        
        return count