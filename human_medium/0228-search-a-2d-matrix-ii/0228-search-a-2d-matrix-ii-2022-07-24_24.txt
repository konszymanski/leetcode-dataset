class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        ans = False
        for i in matrix:
            if target in i:
                ans = True
        return ans