class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        for i in range(len(matrix)):
            is_found = self.bs(matrix[i], target)
            if is_found:
                return True
        return False
    
    def bs(self, nums, target):        
        return target in nums