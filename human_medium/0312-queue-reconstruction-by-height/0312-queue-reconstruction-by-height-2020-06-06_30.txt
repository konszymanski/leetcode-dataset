class Solution:
    def reconstructQueue(self, people: List[List[int]]) -> List[List[int]]:
        return reduce(lambda q, p: q if q.insert(p[1], p) is None else q, sorted(people, key=lambda p: (-p[0], p[1])), [])