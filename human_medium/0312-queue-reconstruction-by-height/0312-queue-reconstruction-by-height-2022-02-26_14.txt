class Solution(object):
    def reconstructQueue(self, people):
        res = []
        for cur in sorted(people, key = lambda p: (-p[0], p[1])):
            res.insert(cur[1], cur)
        return res