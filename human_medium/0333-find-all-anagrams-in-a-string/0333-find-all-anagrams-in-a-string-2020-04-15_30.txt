class Solution:
    def findAnagrams(self, s: str, p: str) -> List[int]:
       N = len(p)
	   target = sorted(list(N))
       res = []
       
        for i in range(N, len(s)+1):
            if sorted(list(s[i-N:i])) == target:
                res.append(i-N)
        return res