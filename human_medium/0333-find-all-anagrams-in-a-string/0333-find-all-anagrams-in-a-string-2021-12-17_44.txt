chp = [0] * 26
        for ch in p:
            chp[ord(ch) - ord(\'a\')] += 1
        left = 0
        res = []
        init = [0] * 26
        for right in range(len(s)):
            init[ord(s[right]) - ord(\'a\')] += 1
            if right - left + 1 == len(p):
                if init == chp:
                    res.append(left)
                init[ord(s[left]) - ord(\'a\')] -= 1
                left += 1
                
        return res