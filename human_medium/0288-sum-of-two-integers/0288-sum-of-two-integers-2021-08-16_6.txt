class Solution:
    def getSum(self, a: int, b: int) -> int:
        MASK = 0xffffffff
        while b:
            carry = a&b
            a = (a ^ b) & MASK
            b = (carry << 1) & MASK
            
        if (a>>31) & 1:
            return ~(a^MASK)
        return a